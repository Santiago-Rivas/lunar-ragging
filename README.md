# lunar-ragging

## Dependencies

To install dependencies, run the following command:

```bash
pip install -r requirements.txt
```

## Execution

To run the code, use the following command:

```bash
python src/rag.py --json data/moon_members.json
```

